name: ccf_timer_low_risk_test
description: Controlled low-risk restoration test.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  go_router: ^17.0.0
  shared_preferences: ^2.0.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/icons/dreamflow_icon.jpg
    - assets/bls/
    - assets/bls/infant/
